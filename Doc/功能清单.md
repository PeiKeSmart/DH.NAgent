# NewLife.Agent 功能清单

> 版本：v1.0 | 日期：2026-07-15

> 状态标记：✅ 已完成 | 🟡 部分完成 | ❌ 未实现 | — 不适用

本清单维护"核心目标 → 功能模块/子功能 → 实现/测试/注释三维状态"。模块编码对齐[需求文档](需求文档.md)第 3 章的功能点名称；详细设计见[架构设计](架构设计.md)和各模块架构文档。

## M1 平台工具类

| 编码 | 功能 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|------|
| M1-1 | `ServiceHelper` 运行时检测 | ✅ | ✅ | ✅ | `GetWorkingDirectory()`/`IsRuntime()`/`SplitCommandLine()`/`ParseTokens()` 支持引号路径和空格；`SplitCommandLine()` 提升为 public 供 DoLoop 复用；17 个测试覆盖 |
| M1-2 | `ServiceModel` / `ServiceConfig` | ✅ | ✅ | ✅ | 测试覆盖构造默认值和属性读写 |
| M1-3 | `SystemdSetting` | ✅ | ✅ | ✅ | 测试覆盖属性 + `Build()` 输出 |
| M1-4 | Windows 平台支持类 | ✅ | ❌ | ✅ | 已补全 `NativeMethods`/`Desktop` 注释；`Advapi32` internal 类型不公开 |

## M2 配置管理

| 编码 | 功能 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|------|
| M2-1 | `Setting` 配置模型 | ✅ | ✅ | ✅ | 18 个测试覆盖全部默认值和读写属性 |
| M2-2 | 服务元数据配置 | ✅ | ✅ | ✅ | 通过 `Setting` 属性测试覆盖 |
| M2-3 | 多实例部署支持 | ✅ | ❌ | ✅ | 不同 `ServiceName` 隔离（设计特性，需集成测试） |
| M2-4 | 配置文件热加载 | ✅ | ❌ | ✅ | `Config<T>` 内置热加载机制（需集成测试） |
| M2-5 | Web 面板专用配置 | ✅ | ✅ | ✅ | `EnableWebPanel`/`WebPort`/`WebAuthLevel`/`WebUserName`/`WebPassword` 默认值测试 |

## M3 服务抽象与跨平台宿主

| 编码 | 功能 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|------|
| M3-1 | `ServiceBase` 基类 | ✅ | ✅ | ✅ | 继承即获得服务治理能力 (`ServiceBase.cs`) |
| M3-2 | Windows 服务（`WindowsService`） | ✅ | ❌ | ✅ | Win32 SCM API 实现 (`WindowsService.cs`) |
| M3-3 | Windows 登录自启动（`WindowsAutorun`） | ✅ | ❌ | ✅ | 注册表 Run 项；已补全 `Install`/`IsInstalled`/`IsRunning` 等 `<returns>` 注释 (`WindowsAutorun.cs`) |
| M3-4 | Linux Systemd | ✅ | ✅ | ✅ | `.service` + `systemctl` (`Systemd.cs`) |
| M3-5 | Linux Procd（OpenWrt） | ✅ | ✅ | ✅ | init 脚本 + procd；`Procd.BuildScript()` 5 个测试覆盖 shebang/参数/脚本结构 (`Procd.cs`) |
| M3-6 | Linux RcInit（传统 init.d） | ✅ | ✅ | ✅ | init.d 脚本；`RcInit.BuildScript()` 5 个测试覆盖头/结构/参数 (`RcInit.cs`) |
| M3-7 | Linux SysVinit | 🟡 | ❌ | ✅ | 状态识别为主；`Init()` 中 SysVinit 检测被注释，不参与自动宿主选择。但 WatchDog 中用作 Systemd 辅助检测 (`SysVinit.cs`) |
| M3-8 | macOS LaunchAgent（`OSXLaunch`） | ✅ | ❌ | ✅ | LaunchAgents plist；已补全 `<returns>` 注释 (`OSXLaunch.cs`) |
| M3-9 | 平台自动检测与适配 | ✅ | ❌ | ✅ | `Init()` 自动选择宿主 |
| M3-10 | 兜底宿主（`DefaultHost`） | ✅ | ✅ | ✅ | 无对应系统服务时的兜底运行 (`DefaultHost.cs`)；`DefaultHostTests` 覆盖默认行为 9 个测试 |

## M4 命令行运维体系

| 编码 | 功能 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|------|
| M4-1 | 命令工厂（`CommandFactory`） | ✅ | ✅ | ✅ | 反射自动发现、命令映射、冲突检测；新增按键/菜单/全部命令枚举测试 |
| M4-2 | 安装服务（`-i`） | ✅ | ✅ | ✅ | 仅安装 (`CommandHandler/Install.cs`) |
| M4-3 | 安装并启动（`-install`） | ✅ | ✅ | ✅ | 组合命令 (`CommandHandler/InstallAndStart.cs`) |
| M4-4 | 重新安装（`-reinstall`） | ✅ | ✅ | ✅ | 测试覆盖 Stop→Remove→Install→Start 完整流程 |
| M4-5 | 卸载服务（`-u`） | ✅ | ✅ | ✅ | 测试覆盖 Process + IsShowMenu |
| M4-6 | 停止并卸载（`-uninstall`） | ✅ | ✅ | ✅ | 测试覆盖 stop→remove 顺序 |
| M4-7 | 启动服务（`-start`） | ✅ | ✅ | ✅ | 测试覆盖 Process + IsShowMenu |
| M4-8 | 停止服务（`-stop`） | ✅ | ✅ | ✅ | 测试覆盖 Process + IsShowMenu |
| M4-9 | 重启服务（`-restart`） | ✅ | ✅ | ✅ | 测试覆盖 Process |
| M4-10 | 状态查询（`-status`） | ✅ | ✅ | ✅ | 测试覆盖构造属性 |
| M4-11 | 模拟运行（`-run`） | ✅ | ✅ | ✅ | 测试覆盖命令属性 |
| M4-12 | 执行服务（`-s`） | ✅ | ✅ | ✅ | 系统服务管理器调用入口；`RunService_Cmd_IsS`/`RunService_Process_CallsHostRun` 测试 |
| M4-13 | 看门狗（`-watch`） | ✅ | ✅ | ✅ | 测试覆盖 Process + IsShowMenu |
| M4-14 | 交互菜单模式 | ✅ | ❌ | ✅ | 无参数时进入菜单循环 |
| M4-15 | 自定义命令扩展 | ✅ | ❌ | ✅ | 继承 `BaseCommandHandler` 自动发现 |

## M5 健康监控与自愈

| 编码 | 功能 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|------|
| M5-1 | 内存阈值监控（`CheckMemory`） | ✅ | ✅ | ✅ | `MaxMemory` 超标自动重启；`ServiceBaseTests.CheckMemory_BelowThreshold_NoRestart` |
| M5-2 | 线程数阈值监控（`CheckThread`） | ✅ | ✅ | ✅ | `MaxThread` 超标自动重启；`ServiceBaseTests.CheckThread_BelowThreshold_NoRestart` |
| M5-3 | 句柄数阈值监控（`CheckHandle`） | ✅ | ✅ | ✅ | `MaxHandle` 超标自动重启（Windows）；`ServiceBaseTests.CheckHandle_BelowThreshold_NoRestart` |
| M5-4 | 定时自动重启（`CheckAutoRestart`） | ✅ | ✅ | ✅ | `AutoRestart` + `RestartTimeRange`；`ServiceBaseTests.CheckAutoRestart_Disabled_NoRestart` |
| M5-5 | WatchDog 看门狗（`CheckWatchDog`） | ✅ | ✅ | ✅ | `CommandHandlerTests.WatchDog_*` 覆盖 Process + IsShowMenu |
| M5-6 | 主动内存整理（`FreeMemory`） | ✅ | ✅ | ✅ | GC + `EmptyWorkingSet`；`ServiceBaseTests.FreeMemory_NoException` |
| M5-7 | 启动后命令（`AfterStart`） | ✅ | ❌ | ✅ | 服务启动后自动执行外部命令；🔍审计: 已修复参数解析支持引号路径（原 `IndexOf(' ')` 不支持 `"C:\Program Files\app.exe" --arg`） |
| M5-8 | 服务启动失败处理 | ✅ | ❌ | ✅ | `StartWork` 单次执行，禁止重入。异常/超时仅日志记录，不重试；🔍审计: 已修复 `Task.Factory.StartNew()` 改用 `Task.Run()`（net40 条件编译降级）(`ServiceBase.cs`) |

## M6 Host 集成扩展

| 编码 | 功能 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|------|
| M6-1 | `ServiceLifetime`（`IHostLifetime`） | ✅ | ✅ | ✅ | 桥接 `ServiceBase` 与 `Microsoft.Extensions.Hosting`；`ServiceLifetimeTests` 覆盖构造/StopWork/StartWork/StopAsync |
| M6-2 | `UseAgentService()` 扩展方法 | ✅ | ✅ | ✅ | HostBuilder 扩展入口；`ServiceLifetimeHostBuilderExtensionsTests` 验证 `IHostLifetime` 注册 |
| M6-3 | `ServiceLifetimeOptions` 配置 | ✅ | ✅ | ✅ | 承载服务元数据配置；`ServiceLifetimeOptionsTests` 覆盖默认值与读写 |
| M6-4 | ASP.NET Core 集成 | ✅ | ❌ | ✅ | 通过 `Zero.Web` 示例验证 |
| M6-5 | Worker Service 集成 | ✅ | ❌ | ✅ | 通过 `TestWorker` 示例验证 |

## M7 Web 管理面板

| 编码 | 功能 | 实现 | 测试 | 注释 | 说明 |
|------|------|:----:|:----:|:----:|------|
| M7-1 | `AgentWebPanel` 内嵌 HTTP 服务器 | ✅ | ❌ | ✅ | 基于 `HttpServer` 轻量实现 |
| M7-2 | RESTful API 状态查询 | ✅ | ✅ | ✅ | `ApiController.Status()`；`ApiControllerTests` 覆盖带/不带 token |
| M7-3 | RESTful API 启停控制 | ✅ | ✅ | ✅ | `ApiController.Control(action)`；`ApiControllerTests` 覆盖未知/空/停止操作 |
| M7-4 | RESTful API 配置读写 | ✅ | ✅ | ✅ | `ApiController.ConfigMetadata()` / `UpdateConfig()`；`ApiControllerTests` 验证返回项排除密码字段 |
| M7-5 | RESTful API 密码修改 | ✅ | ❌ | ✅ | `ApiController.ChangePassword()` |
| M7-6 | RESTful API 日志读取 | ✅ | ✅ | ✅ | `ApiController.Logs()` / `LogFiles()`；`ApiControllerTests` 覆盖无日志文件/负行数 |
| M7-7 | Token 签发与验证 | ✅ | ✅ | ✅ | Bearer Token，24h 过期 |
| M7-8 | 三级鉴权策略 | ✅ | ✅ | ✅ | `None`/`LocalOnly`/`Full`；`AgentWebPanel.ParseAuthLevel()` 测试覆盖大小写不敏感、空值、未知值场景 |
| M7-9 | 登录爆破防护 | ✅ | ✅ | ✅ | `LoginRateLimiter` IP 封锁；`LoginRateLimiterTests` 覆盖封禁阈值/清零/空值/IP隔离 |
| M7-10 | 磁盘 IOPS 监控 | ✅ | ❌ | ✅ | Windows PDH/WMIC + Linux `/proc/diskstats` |
| M7-11 | 扩展面板注册 | ✅ | ✅ | ✅ | `PanelExtension` + `GetExtensions()`；`PanelExtension_Defaults`/`PanelExtension_ReadWrite` 测试覆盖 |
| M7-12 | 运行时凭据更新 | ✅ | ✅ | ✅ | `UpdateCredentials()` 即时生效；`UpdateCredentials_Password_UpdatesImmediately`/`UserNameOnly`/`EmptyUser` 测试 |
| M7-13 | 前端界面（内嵌） | ✅ | ❌ | ✅ | `wwwroot/index.html` + `favicon.ico` |
| M7-14 | 健康指标 API | ✅ | ✅ | ✅ | `ApiController.Health()` 返回内存/线程/句柄/GC 等指标，用于监控系统集成；`ApiControllerTests` 验证返回结构 |
| M7-15 | 面板创建工厂方法 | ✅ | ❌ | ✅ | `ServiceBase.CreateWebPanel()` virtual，子类重写注入自定义面板 |
| M7-16 | 嵌入式资源可替换 | ✅ | ❌ | ✅ | `EmbeddedResourcePrefix` + `EmbeddedResourceAssemblyType` virtual 属性 |

## 统计

| 模块 | 功能数 | 实现完成 | 测试通过 | 注释覆盖 | 综合完成率 |
|------|:------:|:--------:|:--------:|:--------:|:----------:|
| M1 平台工具类 | 4 | 4 | 3 | 4 | 75% |
| M2 配置管理 | 5 | 5 | 3 | 5 | 60% |
| M3 服务抽象与跨平台宿主 | 10 | 9 | 5 | 10 | 50% |
| M4 命令行运维体系 | 15 | 15 | 13 | 15 | 87% |
| M5 健康监控与自愈 | 8 | 8 | 6 | 8 | 75% |
| M6 Host 集成扩展 | 5 | 5 | 3 | 5 | 60% |
| M7 Web 管理面板 | 16 | 16 | 10 | 16 | 63% |
| **总计** | **63** | **62** | **43** | **63** | **68%** |

> 统计说明：「实现完成」列统计 ✅ 标记（M3-7 因 SysVinit 自动选择被注释标记 🟡，计为未完全实现）。
> 「测试通过」列统计有测试覆盖的功能点数，「综合完成率」= 测试通过/功能数。
> 
> 🔍 本次实现审计修复 3 项代码缺陷（AfterStart 引号路径支持、StartLoop TaskScheduler、5 处 XML 注释补全）+ 38 个 CS0618 编译警告 + Procd 静态构造崩溃。
> 
> 🔧 新增 19 个单元测试（M4-12 RunService：2 个、CommandFactory 扩展：4 个、M7-12 UpdateCredentials：3 个、M3-5 Procd.BuildScript：5 个、M3-6 RcInit.BuildScript：5 个），全部通过。
> 
> 🧹 编译警告清零：CS0618(38)→0、CS1571(3)→0、CS0109(1)→0、NU1903 抑制。主项目 + 测试项目 + Hosting 扩展全部 0 警告 0 错误。
> 
> 剩余 19 项测试缺口属于集成测试/平台特定/设计特性范畴，需在对应的集成测试环境覆盖。

## 重构记录（2026-08-01）

> 🧱 **架构重构**：新增 `HealthMonitor` 静态组件，抽取健康检查纯逻辑（阈值判断/自动重启时间判断/时间范围解析），`ServiceBase` 的 `CheckMemory`/`CheckThread`/`CheckHandle`/`CheckAutoRestart` 虚方法签名不变，内部委托该组件；`DoLoop` 内 40 行 AfterStart 引号路径解析复用 `ServiceHelper.SplitCommandLine`（提升为 public）。
> 
> ⚡ **性能优化**：`ApiController.ConfigMetadata` 每次请求全量反射改为静态缓存元数据（属性名/显示名/描述/类型），仅实时读取值。
> 
> 🧹 **代码质量**：清理 46 处空 `<returns>` 注释、修复 `Procd.cs` CS1572/CS1573 警告、删除 void 方法多余 `<returns>` 标签、修正 Systemd/OSXLaunch 参数描述错误；编译警告清零。
> 
> 🔧 **测试补全**：新增 71 个单元测试（138 → 209）：`HealthMonitorTests` 26 个、`DefaultHostTests` 9 个、`ServiceHelperTests` SplitCommandLine 6 个、`ServiceLifetimeTests` 10 个、`ApiControllerTests` 20 个；`UnitTest` 项目新增 Hosting 扩展项目引用。
